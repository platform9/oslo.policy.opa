package identity.list_endpoints

# List endpoints.
# GET  /v3/endpoints
# Intended scope(s): system, project
# "identity:list_endpoints": "rule:admin_required or (role:reader and system_scope:all)"

allow if {}
